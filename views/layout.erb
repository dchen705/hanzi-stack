<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="/favicon.ico">
  <link rel="stylesheet" href="/stylesheets/main.css">
  <title>HSK Flashcards</title>
</head>
<body>
  <header>
    <% if session[:message] %>
      <p><%= session.delete(:message) %></p>
    <% end %>
    <% if signed_in? %>
      <p>Signed in as: <%= session[:username] %></p>
      <form action="/logout" method="post">
        <button type="submit">⬇️ Log out</button>
      </form>
    <% end %>
    <h1>HSK Flashcards</h1>
    <p>This is an app for learning HSK characters.</p>
    <nav>
      <ul>
        <li><a href="/">🏠 Home</a></li>
        <li><a href="/stack">📚 Your Stack</a></li>
        <li><a href="/decks">🎴 View Decks</a></li>
        <% unless signed_in? %>
          <li><a href="/register">📝 Register</a></li>
          <li><a href="/login"> ⬆️ Login</a></li>
        <% end %>
      </ul>
    </nav>
  </header>
  <main>
    <%== yield %>
  </main>
  <footer>
    <p>Made by: </p>
  </footer>
</body>
</html>